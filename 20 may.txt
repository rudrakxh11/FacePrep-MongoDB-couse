ALTER TABLE Worker
ADD CONSTRAINT UQ_LastName UNIQUE(LAST_NAME);

ALTER TABLE Worker
MODIFY DEPARTMENT CHAR(25) NOT NULL;

ALTER TABLE Worker
ADD CONSTRAINT CHK_Department CHECK 
(DEPARTMENT IN ('HR','Admin','Account'));

ALTER TABLE Worker
MODIFY LAST_NAME CHAR(25) NOT NULL UNIQUE;

ALTER TABLE Worker
DROP INDEX UQ_LastName;

ALTER TABLE Worker
ALTER SALARY SET DEFAULT 25000;

ALTER TABLE Worker
ADD PRIMARY KEY (WORKER_ID);

ALTER TABLE Worker
DROP PRIMARY KEY;

ALTER TABLE Worker
ADD CONSTRAINT PK_Worker PRIMARY KEY (WORKER_ID);

CREATE TABLE Employee (
    EMP_ID INT PRIMARY KEY,
    EMP_NAME VARCHAR(50),
    SALARY INT
);

SELECT DES_ENCRYPT('Hello123');

SELECT DES_DECRYPT(DES_ENCRYPT('Hello123'));